# Cluster-specific patch for the Connect UI cert-manager Certificate.
# Copy to certificate.local.yaml and fill in your values. This file is gitignored.

apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: connect-ui-tls
  namespace: connect
spec:
  dnsNames:
    - app.example.cofide.dev
